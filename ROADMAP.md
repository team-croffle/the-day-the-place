# 로드맵

역사 관광 장소(박물관·유적지)와 특별전 정보를 **이 플랫폼 하나에서** 보게 한다.

- **장소**는 TourAPI·국가유산청을 실시간으로 조회한다. 장소 마스터는 DB에 쌓지 않는다.
- **특별전**만 수집해 DB에 두고, 예정·진행만 보여 준다. 끝나면 삭제한다.
- 길찾기처럼 **행동만** 외부 앱으로 보낸다. 정보 본문은 우리 화면에서 본다.

화면 구성은 확정이다. 공통 헤더는 **지도 · 박물관 · 유적지 · 특별전**, 우측은 로그인·회원가입이다.

버전을 올릴 때마다 배포 가능한 상태로 둔다. 핫픽스는 `0.x.y`.

```
골격  →  v0.1 메인·지도  →  v0.2 박물관·유적지  →  v0.3 특별전
                                           →  v0.4 회원  →  v0.5 리뷰  →  v0.6 알림
                                           →  v1.0 추천·안정화
```

지금 저장소는 Vue·Nest·수집기 **골격**까지다. 아래는 그다음 제품 흐름이다.

## v0.1 — 메인 · 지도

공통 셸과 지도가 열린다.

- 헤더·푸터. 내비는 지도·박물관·유적지·특별전
- `/` 메인 — 특별전 히어로, 검색(키워드·지역·시대), 진행중 특별전, 추천 유적지, 시대별 탐방
- `/map` — 왼쪽 검색·유형·지역·목록, 가운데 카카오맵(박물관·유적지 마커), 고르면 팝업과 오른쪽 간이 정보
- **지도 핀은 Tour만.** 박물관·기념관·전시관·역사관광지. 미술관·그 밖 문화시설은 뺀다
- 유형 필터는 이미 받은 결과를 가릴 뿐, 원천을 다시 치지 않는다
- 간이 정보에서 박물관·유적지 상세로 간다. 공식 사이트로 보내지 않는다

## v0.2 — 박물관 · 유적지

목록과 본문. 검색은 각 목록·메인·지도 안에 둔다. 별도 검색 페이지는 없다.

- `/museums` — 검색·필터·그리드. `/museums/:id` — 소개·전시·관람안내·찾아오기
- `/sites` — 유형·시대·유네스코 필터. `/sites/:id` — 소개·볼거리·관람안내·찾아오기, 연표
- 목록·본문은 Tour 장소 단위다. 국가유산은 같은 장소의 **지정 현황**만 상세에 붙인다 (핀을 쪼개지 않음)
- 지도 간이 정보에서 해당 상세로 이동한다
- 상세의 길찾기는 외부 앱으로 보낸다. 저장·공유는 이후 버전

## v0.3 — 특별전

매일 모아 화면에 올린다.

- 수집기가 뉴스 키워드로 전시를 찾고, 제목·기간·기관·소개를 저장한다
- 전시 기간 동안만 기관을 그 행에 붙인다. 끝나면 행을 지운다
- `/exhibitions` — 예정·진행만. 검색·상태·지역, 대표 카드와 그리드
- 메인 히어로·진행중 특별전 섹션에 실제 데이터를 넣는다
- 박물관 상세의 전시 탭에는 그 장소의 살아있는 전시만 붙인다

## v0.4 — 회원 · 마이페이지

계정과 저장.

- 가입·로그인 (JWT)
- 즐겨찾기는 출처 + 원천 장소 ID. 목록·상세·지도에서 저장
- `/me` — 프로필, 즐겨찾기. 방문 기록·리뷰 탭은 v0.5에서 채운다

## v0.5 — 리뷰

장소 방문 기록을 남긴다.

- 회원만 작성, 비회원은 읽지 않는다(마이페이지)
- `/me` 방문 기록·리뷰 탭. 본인 수정·삭제, 길이 제한
- 작성 중 초안은 브라우저에만 둔다
- 장소 상세에는 댓글 스레드를 두지 않는다

## v0.6 — 알림

관심 장소의 새 특별전.

- 즐겨찾기한 장소에 새 전시가 열리면, 로그인 중인 탭으로 알린다 (SSE)
- 탭을 닫으면 `/me` 알림 목록으로 본다
- API 서버는 SSE 때문에 **한 대**로 둔다

## v1.0 — 추천 · 안정화

메인 추천·시대별 탐방과 지도 근처 장소를 실제 추천으로 맞추고, 빈 화면·모바일·배포를 고정한다.
