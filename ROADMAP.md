# 로드맵

역사 관광 장소(박물관·유적지)와 특별전 정보를 **이 플랫폼 하나에서** 보게 한다.

- **장소**는 TourAPI·국가유산청을 실시간으로 조회한다. 장소 마스터는 DB에 쌓지 않는다.
- **특별전**만 수집해 DB에 두고, 예정·진행만 보여 준다. 끝나면 삭제(soft)한다.
- 길찾기처럼 **행동만** 외부 앱으로 보낸다. 정보 본문은 우리 화면에서 본다.

버전을 올릴 때마다 배포 가능한 상태로 둔다. 핫픽스는 `0.x.y`.

```
골격  →  v0.1 지도  →  v0.2 검색·상세  →  v0.3 특별전
                                         →  v0.4 회원  →  v0.5 댓글  →  v0.6 알림
                                         →  v1.0 추천·안정화
```

지금 저장소는 Vue·Nest·수집기 **골격**까지다. 아래는 그다음 제품 흐름이다.

## v0.1 — 메인 · 지도

메인(`/`)과 지도(`/map`)가 각각 열린다.

- 서비스 소개와 지도로 가는 진입
- 카카오맵에 TourAPI·국가유산청 마커를 동시에 띄운다 (먼저 온 쪽부터)
- 유형 필터는 이미 받은 결과를 가릴 뿐, 원천을 다시 치지 않는다
- 마커를 고르면 이름·유형·짧은 소개·주소 간이 정보. 공식 사이트로 보내지 않는다

## v0.2 — 검색 · 장소 상세

목록과 본문.

- `/search`에서 키워드·지역·유형으로 찾는다
- `/places/:id`에 소개·관람 안내·위치를 우리 페이지로 보여 준다
- 두 원천 응답을 같은 화면 스키마로 맞춘다
- 지도 간이 정보에서 상세로 이동한다

## v0.3 — 특별전

매일 모아 화면에 올린다.

- 수집기가 뉴스 키워드로 전시를 찾고, 제목·기간·기관·소개를 저장한다
- 전시 기간 동안만 기관을 그 행에 붙인다. 끝나면 행을 지운다
- `/exhibitions`, `/exhibitions/:id` — 예정·진행만
- 장소 상세에는 그 장소의 살아있는 전시만 붙인다

## v0.4 — 회원 · 즐겨찾기

계정과 저장.

- 가입·로그인 (JWT)
- 즐겨찾기는 출처 + 원천 장소 ID
- `/me`에서 목록 조회·해제

## v0.5 — 댓글

장소와 전시에 의견을 남긴다.

- 회원만 작성, 비회원은 읽기
- 본인 삭제, 길이·빈도 제한
- 작성 중 초안은 브라우저에만 둔다

## v0.6 — 알림

관심 장소의 새 특별전.

- 즐겨찾기한 장소에 새 전시가 열리면, 로그인 중인 탭으로 알린다 (SSE)
- 탭을 닫으면 `/me` 알림 목록으로 본다
- API 서버는 SSE 때문에 **한 대**로 둔다

